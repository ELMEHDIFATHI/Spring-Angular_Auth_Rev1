spring:
  datasource:
    url: jdbc:mysql://localhost:3306/backend?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    password:
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8082